export enum ParamType {
    Query= "query",
    FormData= "parseBody",
    Json= "json",
    TextPlain= "text",
    ArrayBuffer= "arrayBuffer",
}

export enum Gender {
    MALE = "MALE",
    FEMALE = "FEMALE",
    CUSTOM = "CUSTOM",
}

export enum LogType {
    LOGIN = "LOGIN",
    REFRESH_TOKEN = "REFRESH_TOKEN",
    LOGOUT = "LOGOUT",
}